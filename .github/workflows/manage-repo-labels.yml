---
name: Manage Repo Labels

on:
  push:
    branches:
      - main
    paths:
      - .github/repo_labels.yml
      - .github/workflows/manage-repo-labels.yml
  workflow_dispatch:

jobs:
  manage-repo-labels:
    name: Manage Repo Labels
    uses: worganisation/github-config-files/.github/workflows/__manage-repo-labels.yml@0.7.2
    permissions:
      issues: write
